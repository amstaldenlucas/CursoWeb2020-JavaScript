const [a, b, c, d] = [3, 5, 1, 15]

const soma = a + b + c + d
const subtracao = d - b
const multiplicacao = a * b
const divisao = d / a
const modulo = a % 2    // Esse pega o resto da divisão

console.log('Soma =', soma)
console.log('Subtracao =', subtracao)
console.log('Multiplicacao =', multiplicacao)
console.log('Divisao =', -divisao) // Colocando o menos na frente vai mudar o resultado pra negativo
console.log('Modulo =', modulo)